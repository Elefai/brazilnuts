// A new campaign is an operational response to fresh supply or demand,
// not a side effect of a customer completing the current journey.
export const automaticTriggers = new Set([
  "occupancy",
  "table",
  "batch",
]);
